apiVersion: v1
data:
  ZLApp.cfg: |
    // ZLApp Configuration File
    // Application startup configuration for ZLServer pods

    #ifndef _ZL_APP_CONFIG_FILE_IS_INCLUDED
    #define _ZL_APP_CONFIG_FILE_IS_INCLUDED = true

    // Include ZooKeeper configuration
    _zkClientConfig = /opt/ZipLip/ZLZooKeeper/config/zkClient.cfg

    // Site Configuration
    _cfg.site.name = K8S

    // Cluster Configuration - Set via CLUSTER_NAME environment variable
    // Values: MP (zlserver), UI (zlui), SEARCH (zlsearch)
    // _cfg.cluster.name = MP

    // Thread Pool Settings
    _cfg.thread.pool.core = 10
    _cfg.thread.pool.max = 50
    _cfg.thread.pool.queue = 100

    // Memory Settings
    _cfg.memory.min = 512m
    _cfg.memory.max = 4g

    // Logging
    _cfg.log.level = INFO
    _cfg.log.dir = /var/opt/zlserverlogs

    #endif
kind: ConfigMap
metadata:
  annotations:
    kubectl.kubernetes.io/last-applied-configuration: |
      {"apiVersion":"v1","data":{"ZLApp.cfg":"// ZLApp Configuration File\n// Application startup configuration for ZLServer pods\n\n#ifndef _ZL_APP_CONFIG_FILE_IS_INCLUDED\n#define _ZL_APP_CONFIG_FILE_IS_INCLUDED = true\n\n// Include ZooKeeper configuration\n_zkClientConfig = /opt/ZipLip/ZLZooKeeper/config/zkClient.cfg\n\n// Site Configuration\n_cfg.site.name = K8S\n\n// Cluster Configuration - Set via CLUSTER_NAME environment variable\n// Values: MP (zlserver), UI (zlui), SEARCH (zlsearch)\n// _cfg.cluster.name = MP\n\n// Thread Pool Settings\n_cfg.thread.pool.core = 10\n_cfg.thread.pool.max = 50\n_cfg.thread.pool.queue = 100\n\n// Memory Settings\n_cfg.memory.min = 512m\n_cfg.memory.max = 4g\n\n// Logging\n_cfg.log.level = INFO\n_cfg.log.dir = /var/opt/zlserverlogs\n\n#endif\n"},"kind":"ConfigMap","metadata":{"annotations":{},"name":"zlapp-config","namespace":"default"}}
  creationTimestamp: "2025-12-22T18:16:39Z"
  name: zlapp-config
  namespace: default
  resourceVersion: "2111445"
  uid: 3ab6743d-7e83-4afd-a21c-5abfa21a4409
