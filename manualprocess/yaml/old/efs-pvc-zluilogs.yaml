apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zluilogs-efs
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 10Gi
  volumeName: efs-pv-zluilogs
